{# src/Acme/DemoBundle/Resources/views/Form/fields.html.twig #}
{% extends 'form_div_layout.html.twig' %}

{% block file_widget %}
    {% spaceless %}
        {{ block('form_widget') }}
        {% if image_url|default(null) is not null %}
            <img src="{{ image_url | imagine_filter('gallery_thumb') }}"/>
        {% endif %}

    {% endspaceless %}
{% endblock %}